"""pyutil — shared utilities for LLM/AI Python scripts.

Currently exposes:
- credits.emit_cost — emit STDERR COST: line for billing meter
- credits.is_draft_quality — degraded-mode env flag
- credits.reservation_key, credits.correlation_id — passed in from C#
"""

